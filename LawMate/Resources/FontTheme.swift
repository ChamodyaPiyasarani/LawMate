import SwiftUI

extension Font {
    // Hero huge title — "Justice, Refined."
    static let lmHero    = Font.largeTitle.bold()
    // Screen title — "Login", "Sign UP"
    static let lmTitle   = Font.title.bold()
    // Section heading — "My Cases"
    static let lmHeading = Font.headline.weight(.semibold)
    // Body text
    static let lmBody    = Font.body
    // Small body / captions
    static let lmCaption = Font.caption
    // Button label
    static let lmButton  = Font.callout.weight(.semibold)
    // Tab bar label
    static let lmTab     = Font.caption2.weight(.medium)
    // Brand "LAWMATE" wordmark
    static let lmBrand   = Font.headline.weight(.heavy)
    // Brand tagline
    static let lmTagline = Font.caption.weight(.regular)
    // Input placeholder / field text
    static let lmField   = Font.body
}
