import SwiftUI

extension Font {
    // Hero huge title — "Justice, Refined."
    static let lmHero    = Font.system(size: 34, weight: .bold)
    // Screen title — "Login", "Sign UP"
    static let lmTitle   = Font.system(size: 28, weight: .bold)
    // Section heading — "My Cases"
    static let lmHeading = Font.system(size: 20, weight: .semibold)
    // Body text
    static let lmBody    = Font.system(size: 16, weight: .regular)
    // Small body / captions
    static let lmCaption = Font.system(size: 13, weight: .regular)
    // Button label
    static let lmButton  = Font.system(size: 16, weight: .semibold)
    // Tab bar label
    static let lmTab     = Font.system(size: 11, weight: .medium)
    // Brand "LAWMATE" wordmark
    static let lmBrand   = Font.system(size: 20, weight: .heavy)
    // Brand tagline
    static let lmTagline = Font.system(size: 12, weight: .regular)
    // Input placeholder / field text
    static let lmField   = Font.system(size: 15, weight: .regular)
}
